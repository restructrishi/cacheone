/**
 * Semantic theme token names.
 * Actual values are in CSS variables (styles/global.css) for dark/light.
 */
export const THEME_STORAGE_KEY = 'cacheone-theme';

export const THEMES = {
  DARK: 'dark',
  LIGHT: 'light',
};

export const DEFAULT_THEME = THEMES.DARK;
